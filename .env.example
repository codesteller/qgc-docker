# Environment configuration for QGroundControl Docker Compose
# Copy this file to .env and customize as needed

# User configuration (matches host user to avoid permission issues)
UID=1000
GID=1000

# QGroundControl download URL (can be overridden for specific versions)
QGC_DOWNLOAD_LINK=https://d176tv9ibo4jno.cloudfront.net/latest/QGroundControl-x86_64.AppImage

# Display configuration (usually auto-detected)
# DISPLAY=:0

# X11 Authentication (usually auto-detected)
# XAUTHORITY=/home/user/.Xauthority